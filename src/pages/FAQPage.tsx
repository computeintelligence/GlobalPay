import React, { useState } from 'react';

const faqs = [['What is GlobalPay?', 'GlobalPay connects people and businesses to trusted payment partners to process payments.'], ['What is the difference between Personal and Business?', 'Personal is for individuals. Business is for businesses that accept customer payments. They are separate accounts.'], ['Do I need to verify my identity?', 'Yes. Everyone completes identity verification before using the service.'], ['What does it cost?', 'The GlobalPay fee is shown as a percentage with a small minimum and a maximum, plus partner fees.'], ['How is my fee worked out?', 'The estimate shows the payment partner fee, VAT on partner fee, GlobalPay fee and total.'], ['Why is the international fee an estimate?', 'International fees depend on the currency pair and payment method. The final amount is confirmed before you pay.'], ['Are crypto fees different?', 'Crypto fees include network or partner costs shown before you confirm. Crypto prices can change.'], ['Which currencies and payment methods are supported?', '[ADD SUPPORTED CURRENCIES] and [ADD SUPPORTED PAYMENT METHODS]'], ['Can I use one account for both Personal and Business?', 'No. Personal and Business are separate accounts with separate signup and onboarding.']];

export const FAQPage: React.FC = () => { const [open, setOpen] = useState<number | null>(null); return <div className="mx-auto max-w-4xl px-4 pt-28 pb-20 sm:px-6 lg:px-8"><h1 className="text-4xl font-black text-slate-900 dark:text-white sm:text-5xl">Frequently asked questions</h1><div className="mt-10 space-y-3">{faqs.map(([question, answer], index) => <div key={question} className="rounded-2xl border border-slate-200 bg-white dark:border-slate-800 dark:bg-[#07132B]"><button onClick={() => setOpen(open === index ? null : index)} className="w-full p-5 text-left font-bold text-slate-900 dark:text-white">{question}</button>{open === index && <p className="px-5 pb-5 text-sm leading-relaxed text-slate-600 dark:text-slate-300">{answer}</p>}</div>)}</div></div>; };
