import React, { useState } from 'react';
const faqs = [['What is GlobalPay?', 'GlobalPay is a multi-currency payment platform, including crypto, for sending and receiving payments.'], ['What is the difference between Personal and Business?', 'Personal is for individuals. Business is for merchants that accept customer payments on a website, app, or online store. They are separate accounts.'], ['Do I need to verify my identity?', 'Yes. Personal users complete identity verification. Businesses complete business verification before making or receiving payments.'], ['Does GlobalPay hold my money?', 'No. GlobalPay does not hold funds. Trusted payment partners move the money and GlobalPay records each transaction.'], ['What does it cost?', 'The fee is less than 3% per transaction for Personal and Business accounts.'], ['Which currencies and countries are supported?', '[ADD SUPPORTED LIST]'], ['How do businesses connect GlobalPay to their store or app?', 'Businesses connect GlobalPay using our API, test before going live, and launch once verified.'], ['Can I use one account for both Personal and Business?', 'No. Personal and Business are separate accounts with separate signup and onboarding.']];
export const FAQPage: React.FC = () => { const [open, setOpen] = useState<number | null>(null); return <div className="pt-28 pb-20 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8"><h1 className="text-4xl sm:text-5xl font-black text-slate-900 dark:text-white">Frequently asked questions</h1><div className="mt-10 space-y-3">{faqs.map(([question, answer], index) => <div key={question} className="rounded-2xl border border-slate-200 dark:border-slate-800 bg-white dark:bg-[#07132B]"><button onClick={() => setOpen(open === index ? null : index)} className="w-full text-left p-5 font-bold text-slate-900 dark:text-white">{question}</button>{open === index && <p className="px-5 pb-5 text-sm leading-relaxed text-slate-600 dark:text-slate-300">{answer}</p>}</div>)}</div></div>; };
