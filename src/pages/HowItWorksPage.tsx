import React, { useState } from 'react';
import { Check } from 'lucide-react';

const personal = ['Create a Personal account', 'Verify your identity with an ID and selfie', 'Send or receive payments'];
const business = ['Create a Business account', 'Verify your business and its directors', 'Get API access and start accepting payments'];

export const HowItWorksPage: React.FC = () => { const [type, setType] = useState<'personal' | 'business'>('personal'); const steps = type === 'personal' ? personal : business; return <div className="pt-28 pb-20 max-w-5xl mx-auto px-4 sm:px-6 lg:px-8"><div className="text-center"><h1 className="text-4xl sm:text-5xl font-black text-slate-900 dark:text-white">How it works</h1><p className="mt-4 text-lg text-slate-600 dark:text-slate-300">GlobalPay connects you to trusted payment partners that move the money. GlobalPay does not hold funds.</p></div><div className="mt-10 flex justify-center gap-2"><button onClick={() => setType('personal')} className={`px-5 py-3 rounded-xl font-bold ${type === 'personal' ? 'bg-[#0052FF] text-white' : 'bg-slate-100 dark:bg-slate-800 text-slate-600 dark:text-slate-300'}`}>Personal</button><button onClick={() => setType('business')} className={`px-5 py-3 rounded-xl font-bold ${type === 'business' ? 'bg-[#0052FF] text-white' : 'bg-slate-100 dark:bg-slate-800 text-slate-600 dark:text-slate-300'}`}>Business</button></div><div className="mt-10 grid md:grid-cols-3 gap-5">{steps.map((step, index) => <div key={step} className="p-6 rounded-2xl bg-white dark:bg-[#07132B] border border-slate-200 dark:border-slate-800"><span className="text-3xl font-black text-[#0052FF]">0{index + 1}</span><Check className="w-5 h-5 text-emerald-500 mt-6" /><p className="mt-3 font-bold text-slate-900 dark:text-white">{step}</p></div>)}</div></div>; };
